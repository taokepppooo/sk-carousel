import type { SKContainerOptionsType } from '../options'
function containerDraw(
  container: HTMLElement | undefined,
  options: SKContainerOptionsType | undefined
) {
  //
  console.log(options)
}

export { containerDraw }
