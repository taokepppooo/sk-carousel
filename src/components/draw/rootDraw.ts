import type { SKRootOptionsType } from '../options'
function rootDraw(
  root: HTMLElement | undefined,
  options: SKRootOptionsType | undefined
) {
  console.log(options)
  //
}

export { rootDraw }
